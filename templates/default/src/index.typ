#import "/site.typ": site

#show: site

= Aster is ready.

Edit `src/index.typ`, then run `aster build` to generate this page.

== Typst remains available.

You can use Typst markup, scripting, mathematics such as $e^(i pi) + 1 = 0$,
and HTML elements in the same source file.
