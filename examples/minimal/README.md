# Minimal Aster example

This is Aster's smallest complete project. It intentionally contains one route
and no framework-owned styling.

```sh
cargo run -- dev -p examples/minimal
```

Earlier revisions were informed by an upstream starter; its notice is preserved
in `UPSTREAM-LICENSE`.
