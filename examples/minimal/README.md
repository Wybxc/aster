# Minimal Aster example

This is the Aster port of Astro's `minimal` starter. It intentionally contains
one route and no framework-owned styling.

```sh
cargo run -- dev -p examples/minimal
```

The source design is distributed under `ASTRO-LICENSE`.
