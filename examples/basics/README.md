# Aster basics

This is an Aster port of Astro's `basics` starter. The route, layout, component,
component-owned CSS, and project assets are kept in separate source modules.

```sh
cargo run -- dev -p examples/basics
```

The visual design and copied assets are distributed under `ASTRO-LICENSE`.
