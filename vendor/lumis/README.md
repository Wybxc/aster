# Lumis language data

Synchronized from `leandrocp/lumis` 0.12.1 (`3507439b896fe281b878a830c7ccc25fac555ed8`).

Run `scripts/sync-lumis.sh` to resynchronize the queries and license.
`languages.toml` is Aster runtime configuration and is preserved.
